name: Publicar Release Windows
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - uses: gradle/actions/setup-gradle@v3

      - name: gradle build
        run: ./gradlew clean build

      - name: version
        id: get_version
        shell: bash
        run: |
          VERSION=$(./gradlew properties -q | grep "^version:" | awk '{print $2}')
          echo "APP_VERSION=$VERSION" >> $GITHUB_ENV

      - name: jpackage
        shell: bash
        run: |
          jpackage --input build/libs \
            --name "xppc" \
            --main-jar "xppc-${{ env.APP_VERSION }}-all.jar" \
            --main-class br.ifpe.edu.AppMain \
            --type app-image \
            --dest dist

      - name: zip release
        run: Compress-Archive -Path dist/xppc -DestinationPath "xppc-windows-${{ env.APP_VERSION }}.zip"

      - name: create release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ env.APP_VERSION }}" "xppc-windows-${{ env.APP_VERSION }}.zip" \
            --title "XPPC ${{ env.APP_VERSION }}" \